<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="LARGA.MobileApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:auth="clr-namespace:LARGA.MobileApp.Views.Auth"
    xmlns:driver="clr-namespace:LARGA.MobileApp.Views.Driver"
    xmlns:manager="clr-namespace:LARGA.MobileApp.Views.Manager"
    Title="LARGA.MobileApp"
    FlyoutBehavior="Disabled"
    Shell.TabBarBackgroundColor="White"
    Shell.TabBarTitleColor="Black"
    Shell.TabBarUnselectedColor="#999999"
    Shell.TabBarForegroundColor="Black">

    <!-- The initial startup page -->
    <ShellContent
        Title="Welcome"
        ContentTemplate="{DataTemplate auth:LandingPage}"
        Route="landing"
        Shell.NavBarIsVisible="False" />

    <!-- Driver Dashboard with Bottom Tabs -->
    <TabBar Route="driver-dashboard">
        <Tab Route="home" Title="Home" Icon="car.png">
            <ShellContent ContentTemplate="{DataTemplate driver:DriverDashboardPage}" />
        </Tab>
        <Tab Title="Ledger" Icon="ledger.png">
            <ShellContent ContentTemplate="{DataTemplate driver:LedgerPage}" />
        </Tab>
        <Tab Title="Reports" Icon="report.png">
            <ShellContent ContentTemplate="{DataTemplate driver:ReportsPage}" />
        </Tab>
        <Tab Title="Profile" Icon="profile.png">
            <ShellContent ContentTemplate="{DataTemplate driver:ProfilePage}" />
        </Tab>
    </TabBar>

    <!-- Manager Dashboard with Bottom Tabs -->
    <TabBar Route="manager-dashboard">
        <Tab Route="home" Title="Map" Icon="car.png">
            <ShellContent ContentTemplate="{DataTemplate manager:ManagerDashboardPage}" />
        </Tab>
        <Tab Title="Chats" Icon="send_icon.png">
            <ShellContent ContentTemplate="{DataTemplate manager:ManagerChatsPage}" />
        </Tab>
        <Tab Title="Ledger" Icon="ledger.png">
            <ShellContent ContentTemplate="{DataTemplate manager:ManagerLedgerPage}" />
        </Tab>
        <Tab Title="Alerts" Icon="warning_icon.png">
            <ShellContent ContentTemplate="{DataTemplate manager:AlertCenterPage}" />
        </Tab>
        <Tab Title="Profile" Icon="profile.png">
            <ShellContent ContentTemplate="{DataTemplate manager:ManagerProfilePage}" />
        </Tab>
    </TabBar>
</Shell>