<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LARGA.MobileApp.ViewModels.Driver"
             x:Class="LARGA.MobileApp.Views.Driver.ProfilePage"
             x:DataType="vm:ProfileViewModel"
             BackgroundColor="{StaticResource LargaWhite}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label Text="My Profile" FontSize="32" Style="{StaticResource HeaderStyle}" TextColor="{StaticResource LargaDarkText}" Margin="0,10,0,0" />

            <Border BackgroundColor="{StaticResource LargaLightBlue}" StrokeShape="RoundRectangle 20" StrokeThickness="0" Padding="20">
                <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                    <Border StrokeShape="RoundRectangle 75"
                            HeightRequest="150"
                            WidthRequest="150"
                            BackgroundColor="{StaticResource LargaPrimaryBlue}"
                            StrokeThickness="0">
                        <Image Source="{Binding ProfileImageUrl}" Aspect="AspectFill" />
                    </Border>

                    <Label Text="{Binding FullName}"
                           FontSize="22"
                           Style="{StaticResource HeaderStyle}"
                           TextColor="{StaticResource LargaDarkText}"
                           HorizontalOptions="Center" />

                    <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="15" HorizontalOptions="Center">
                        <Label Grid.Column="0" Text="Contact #:" FontSize="20" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="{Binding ContactNumber}" FontSize="20" TextColor="{StaticResource LargaDarkText}" FontFamily="DMMonoMedium" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="{StaticResource LargaLightBlue}" StrokeShape="RoundRectangle 20" StrokeThickness="0" Padding="15">
                <VerticalStackLayout Spacing="12">
                    <Label Text="LICENSE &amp; CREDENTIALS" FontSize="18" Style="{StaticResource HeaderStyle}" TextColor="{StaticResource LargaDarkText}" />

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="20">
                        <Label Grid.Column="0" Text="LTO License No:" FontSize="16" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="{Binding LicenseNumber}" FontSize="16" TextColor="{StaticResource LargaDarkText}" HorizontalTextAlignment="End" FontFamily="DMMonoMedium" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="#1A1A1A" />

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="20">
                        <Label Grid.Column="0" Text="Expiry Date:" FontSize="16" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="{Binding ExpiryDateDisplay}" FontSize="16" TextColor="{StaticResource LargaDarkText}" HorizontalTextAlignment="End" FontFamily="DMMonoMedium" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="#1A1A1A" />

                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                        <Label Grid.Column="0" Text="Status:" FontSize="16" TextColor="{StaticResource LargaDarkText}" />
                        <Border Grid.Column="1"
                                StrokeShape="RoundRectangle 12"
                                HeightRequest="24"
                                WidthRequest="24"
                                BackgroundColor="{Binding StatusColor}"
                                HorizontalOptions="End"
                                VerticalOptions="Center"
                                StrokeThickness="0" />
                        <Label Grid.Column="2" Text="{Binding StatusText}" FontSize="16" TextColor="{StaticResource LargaDarkText}" FontFamily="DMMonoMedium" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="#1A1A1A" />

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="20">
                        <Label Grid.Column="0" Text="DL Codes:" FontSize="16" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="{Binding DlCodes}" FontSize="16" TextColor="{StaticResource LargaDarkText}" HorizontalTextAlignment="End" FontFamily="DMMonoMedium" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="{StaticResource LargaLightBlue}" StrokeShape="RoundRectangle 20" StrokeThickness="0" Padding="15">
                <VerticalStackLayout Spacing="12">
                    <Label Text="MY PERFORMANCE STANDING" FontSize="18" Style="{StaticResource HeaderStyle}" TextColor="{StaticResource LargaDarkText}" />

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="20">
                        <Label Grid.Column="0" Text="Payment Reliability:" FontSize="16" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="{Binding PaymentReliability}" FontSize="16" TextColor="{StaticResource LargaDarkText}" HorizontalTextAlignment="End" FontFamily="DMMonoMedium" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="#1A1A1A" />

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="20">
                        <Label Grid.Column="0" Text="Shift Punctuality:" FontSize="16" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="{Binding ShiftPunctualityDisplay}" FontSize="16" TextColor="{StaticResource LargaDarkText}" HorizontalTextAlignment="End" FontFamily="DMMonoMedium" />
                    </Grid>

                    <BoxView HeightRequest="1" Color="#1A1A1A" />

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="20">
                        <Label Grid.Column="0" Text="Damage History:" FontSize="16" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="{Binding DamageHistoryDisplay}" FontSize="16" TextColor="{StaticResource LargaDarkText}" HorizontalTextAlignment="End" FontFamily="DMMonoMedium" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border BackgroundColor="{StaticResource LargaLightBlue}" StrokeShape="RoundRectangle 20" StrokeThickness="0" Padding="15">
                <VerticalStackLayout Spacing="10">
                    <Label Text="ACCOUNT SETTINGS" FontSize="18" Style="{StaticResource HeaderStyle}" TextColor="{StaticResource LargaDarkText}" />

                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="5,3">
                        <Label Grid.Column="0" Text="›" FontSize="30" VerticalOptions="Center" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="Change password" FontSize="16" TextColor="{StaticResource LargaDarkText}" VerticalOptions="Center" />
                        <Label Grid.Column="2" Text="❯" FontSize="16" TextColor="{StaticResource LargaDarkText}" VerticalOptions="Center" />
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding ChangePasswordCommand}" />
                        </Grid.GestureRecognizers>
                    </Grid>

                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="5,3">
                        <Label Grid.Column="0" Text="›" FontSize="30" VerticalOptions="Center" TextColor="{StaticResource LargaDarkText}" />
                        <Label Grid.Column="1" Text="Update Contact Number" FontSize="16" TextColor="{StaticResource LargaDarkText}" VerticalOptions="Center" />
                        <Label Grid.Column="2" Text="❯" FontSize="16" TextColor="{StaticResource LargaDarkText}" VerticalOptions="Center" />
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding UpdateContactNumberCommand}" />
                        </Grid.GestureRecognizers>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Button Text="LOG OUT"
                    Command="{Binding LogoutCommand}"
                    BackgroundColor="#FF6666"
                    TextColor="{StaticResource LargaDarkText}"
                    CornerRadius="36"
                    HeightRequest="72"
                    FontSize="22"
                    Style="{StaticResource HeaderStyle}"
                    Margin="0,10,0,20" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
